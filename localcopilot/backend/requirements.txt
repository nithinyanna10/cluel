# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# Database
sqlalchemy>=2.0.0

# Config / validation
pydantic>=2.6.0
pydantic-settings>=2.2.0

# HTTP client (Ollama / async LLM calls)
httpx>=0.27.0

# LLM providers (install whichever you use)
anthropic>=0.25.0
openai>=1.20.0

# ── Optional heavy dependencies ────────────────────────────────────────────────
# Uncomment and install manually to enable transcription / OCR.
# They are NOT required for the app to run (mock fallbacks are built-in).

# Transcription
# faster-whisper>=1.0.0

# OCR
# easyocr>=1.7.0
# Pillow>=10.0.0   # required by easyocr

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0   # TestClient dependency
